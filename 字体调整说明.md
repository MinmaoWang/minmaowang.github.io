# ✅ 字体大小调整说明

## 本次更新

### 调整全局字体大小 📝

**修改前：**
- 使用浏览器默认字体大小（通常为 16px）
- 内容显示较少，页面看起来比较空

**修改后：**
- 全局字体大小：**14px** ✅
- 内容显示更多，布局更紧凑

## 修改的文件

```
assets/css/global.css
```

**具体修改：**
```css
body {
    font-family: "Lato", sans-serif;
    font-weight: 400;
    font-size: 14px;  ← 新增
    padding-top: 5.0rem;
}
```

## 效果预览

### 调整后的优势：

✅ **显示更多内容**：同样的屏幕空间可以显示更多信息  
✅ **布局更紧凑**：页面看起来更充实，不再显得空旷  
✅ **保持可读性**：14px 仍然是一个非常舒适的阅读大小  
✅ **专业感更强**：紧凑的布局更符合学术网站风格  

### 适用范围：

这个字体大小调整会影响：
- 📄 论文列表的描述文字
- 📰 新闻动态的内容
- 💼 实习经历的描述
- 🎓 教育背景的详细信息
- 📝 个人简介的段落文字

### 不受影响的部分：

以下元素保持原有大小：
- 标题（h1, h2, h3等）- 保持相对大小
- 导航栏 - 保持原有大小
- 按钮和标签 - 保持原有大小

## 🚀 查看效果

```bash
cd /Users/wangminmao/Desktop/my_domain/pspage/minmaowang.github.io

# 清除缓存并重新构建
rm -rf _site

# 启动服务器
bundle exec jekyll serve
```

访问：http://localhost:4000

**提示**：如果浏览器有缓存，请按 `Ctrl+Shift+R`（Windows/Linux）或 `Cmd+Shift+R`（Mac）强制刷新。

## 💡 进一步调整（可选）

如果你觉得：

### 字体还是太大
可以改为 13px：
```css
body {
    font-size: 13px;
}
```

### 字体太小了
可以改为 15px：
```css
body {
    font-size: 15px;
}
```

### 只想调整特定部分

例如只调整论文列表：
```css
.publication-item {
    font-size: 13px;
}
```

或只调整新闻：
```css
.news-item {
    font-size: 13px;
}
```

## 📊 字体大小对比

| 位置 | 之前 | 现在 | 变化 |
|------|------|------|------|
| 正文 | 16px | 14px | ↓ 12.5% |
| h1 标题 | ~32px | ~28px | ↓ 按比例 |
| h2 标题 | ~24px | ~21px | ↓ 按比例 |
| h3 标题 | ~18px | ~16px | ↓ 按比例 |

---

**现在你的主页可以显示更多内容了！** 🎉

字体更紧凑，但依然保持良好的可读性！

