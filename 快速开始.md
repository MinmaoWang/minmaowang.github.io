# 🚀 快速开始指南

## 第一步：安装依赖

```bash
# 安装 Jekyll（如果还没有安装）
gem install jekyll bundler

# 进入项目目录
cd /Users/wangminmao/Desktop/my_domain/pspage/minmaowang.github.io

# 安装项目依赖
bundle install
```

## 第二步：本地预览

```bash
# 启动本地服务器
bundle exec jekyll serve

# 然后在浏览器访问：http://localhost:4000
```

## 第三步：推送到 GitHub

```bash
# 添加所有更改
git add .

# 提交更改
git commit -m "更新为学术主页模板"

# 推送到 GitHub
git push origin main
```

注意：如果分支名是 master 而不是 main，请使用：
```bash
git push origin master
```

## 第四步：在 GitHub 上启用 Pages

1. 访问你的仓库：https://github.com/minmaowang/minmaowang.github.io
2. 点击 **Settings**（设置）
3. 在左侧菜单找到 **Pages**
4. 在 Source 下选择 **Deploy from a branch**
5. Branch 选择 **main**（或 **master**）
6. 点击 **Save**

等待 2-5 分钟后，你的网站就会在 https://minmaowang.github.io 上线！

## 常见问题

### Q: 如何修改个人信息？
A: 编辑 `_data/profile.yml` 文件

### Q: 如何添加新论文？
A: 在 `_publications/2024/` 目录下创建新的 `.md` 文件，参考现有文件格式

### Q: 如何添加新闻动态？
A: 在 `_news/` 目录下创建新的 `.md` 文件

### Q: 如何更换照片？
A: 替换 `assets/images/profile.jpg` 文件

### Q: 本地预览时报错？
A: 尝试删除 `Gemfile.lock` 然后重新运行 `bundle install`

## 📧 需要帮助？

如果遇到问题，可以：
1. 查看 [academic-homepage 文档](https://github.com/luost26/academic-homepage)
2. 发送邮件到：wmmmm@tju.edu.cn

